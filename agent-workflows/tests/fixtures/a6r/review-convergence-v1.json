{
  "schema": "a6r-review-convergence/v1",
  "branches": {
    "duplicate": {"disposition": "duplicate", "canonical_finding": "finding-required"},
    "approved_choice": {"disposition": "reject", "reason": "approved decision"},
    "immaterial": {"disposition": "defer", "reason": "immaterial improvement"},
    "required": {"disposition": "required", "canonical_finding": "finding-required"},
    "needs_user": {"disposition": "needs-user", "branch": "stopped"},
    "time_exhausted": {"terminal_reason": "time_exhausted"},
    "rounds_exhausted": {"terminal_reason": "rounds_exhausted"},
    "attempts_exhausted": {"terminal_reason": "attempts_exhausted"},
    "guarded_reopen": {"approval": "approved-reopen", "replacement_budget": "v2", "receipt_binding": ["approval_id", "operation", "run_id", "replacement_budget", "replacement_budget_digest", "expected_head"]}
  }
}
