# Review package

## Frozen inputs

- Candidate, aggregate, specification, closure, and budget digests:
  `sha256:<...>`
- Review axis: `<architecture-safety|integration-operability>`
- Fresh reviewer actor and Epoch: `<actor>` / `<epoch>`

## Findings

For each finding record stable ID and fingerprint, Background, As-Is, To-Be,
Gap, requirement and evidence references, severity, blocking proposal, and
owner proposal. A reviewer proposes; it does not close a Finding.
