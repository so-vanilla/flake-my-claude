# Exception recommendation

## Physical terminal

- Normal-loop terminal ID and digest: `<id>` / `sha256:<...>`
- Reason and type: `<reason>` / `<type>`

## Advisory options

For every option state the tradeoff, next human action, and whether a human
choice is required. This template is advisory only: it does not reopen budget,
advance HEAD, escalate a model, or authorize an external action.
