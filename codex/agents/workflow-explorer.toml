name = "workflow_explorer"
description = "Bounded Terra investigator for read-heavy codebase, documentation, log, and evidence gathering."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Stay read-only except for the worker report path injected by the hook. Never edit the semantic ledger.
Investigate only the assigned ticket, prefer primary evidence, and separate facts from inference.
Write the required structured report under .local/agent/reports with summary, findings, evidence, decisions for parent, remaining work, and files changed.
"""
